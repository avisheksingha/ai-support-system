# ============================================
# K8S EUREKA SERVER CONFIGURATION
# ============================================
# In Kubernetes, prefer IP addresses for service registration stability
eureka.instance.prefer-ip-address=true

# Explicitly bind the hostname to the Kubernetes service FQDN
eureka.instance.hostname=discovery-service.ai-support.svc.cluster.local

# ===============================================
# DATABASE CONFIGURATION (Docker)
# ===============================================
spring.datasource.url=jdbc:postgresql://postgres:5432/routing_db
spring.datasource.username=postgres
spring.datasource.password=pass

# Initialize the database schema and data on startup
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# ============================================
# JPA/HIBERNATE CONFIGURATION (Docker)
# ============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jackson.time-zone=UTC

# ===============================================
# EUREKA CLIENT CONFIGURATION (Docker)
# ===============================================
eureka.client.service-url.defaultZone=http://discovery-service:8761/eureka
eureka.instance.hostname=routing-service
eureka.instance.prefer-ip-address=true

# ============================================
# KAFKA CONFIGURATION (Docker)
# ============================================
spring.kafka.bootstrap-servers=kafka:9092

# --- Consumer ---
spring.kafka.consumer.group-id=routing-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false

# --- Consumer Deserializer Settings (consumes from ticket-analyzed) ---
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# --- Consumer Performance Tuning ---
spring.kafka.consumer.properties.max.poll.interval.ms=300000
spring.kafka.consumer.properties.max.poll.records=10

# --- Listener ---
spring.kafka.listener.ack-mode=record
spring.kafka.listener.concurrency=2
spring.kafka.listener.missing-topics-fatal=false

# --- Producer (publishes to ticket-routed) ---
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# --- Prevent producer from embedding its own package type header ---
spring.kafka.producer.properties.spring.json.add.type.headers=false

# --- Producer Reliability Settings ---
spring.kafka.producer.acks=all
spring.kafka.producer.retries=5
spring.kafka.producer.properties.enable.idempotence=true
spring.kafka.producer.properties.max.in.flight.requests.per.connection=5

# --- Producer Timeout Settings ---
spring.kafka.producer.properties.delivery.timeout.ms=120000
spring.kafka.producer.properties.request.timeout.ms=30000
spring.kafka.producer.properties.retry.backoff.ms=1000
spring.kafka.producer.properties.linger.ms=10
# ===============================================
# LOGGING CONFIGURATION (Docker)
# ===============================================
logging.level.root=INFO
logging.level.com.aisupport=INFO
logging.level.org.springframework.web=INFO
spring.mvc.log-request-details=false

# ============================================
# DATABASE CONFIGURATION (Kubernetes)
# ============================================
spring.datasource.url=jdbc:postgresql://postgres.ai-support.svc.cluster.local:5432/analysis_db
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# ============================================
# EUREKA CLIENT CONFIGURATION (Kubernetes)
# ============================================
eureka.client.service-url.defaultZone=http://discovery-service.ai-support.svc.cluster.local:8761/eureka
eureka.instance.prefer-ip-address=true

# ============================================
# KAFKA CONFIGURATION (Kubernetes)
# ============================================
spring.kafka.bootstrap-servers=kafka.ai-support.svc.cluster.local:9092
